import { MEDICAL_COMPLIANCE_SYSTEM_PROMPT } from '@/content/lib/medical-compliance';

/**
 * 게시 사이트 = '구글' 선택 시 사용하는 프롬프트 빌더 모음.
 *
 * - 네이버용 프롬프트(generate-content / generate-titles 라우트 내 인라인 리터럴)는
 *   기존 사용자 품질 회귀 방지를 위해 절대 수정하지 않고, 구글 분기만 이 파일로 분리.
 * - 출력 서식 규칙(마크다운 금지, H2 독립 줄, ▶ H3, [이미지 N: ...], [핵심 요약]/[자주 묻는 질문] 블록)은
 *   네이버와 동일하게 유지 — 다운스트림(마크다운 클리닝, ContentPreview 파서, seo/geo 분석)이
 *   동일 규칙을 전제로 동작하기 때문.
 * - 의료광고법(의료법 제56조) 준수 지시는 네이버와 동일 강도로 적용 (절대 약화 금지).
 */

export interface GoogleContentPromptParams {
  title: string;
  keyword: string;
  hospitalType: string;
  additionalInfo: string;
  writingStyleGuide: string;
  writingStyleLabel: string;
  formatGuide: string;
  longtailKeywords: string[];
  competitorText: string;
  region: string;
  hospitalName: string;
  isGeoMode: boolean;
  /** DUMBIFY 난이도 지시 — 'easy'면 L1 균형 블록, 'standard'면 빈 문자열 (시점과 독립) */
  readabilityGuide: string;
  /** VOICE-DNA 문체 카드 프롬프트 블록 — 카드 없거나 토글 OFF면 빈 문자열 (기존 동작 유지) */
  voiceDnaBlock?: string;
  /** VIRAL-HOOKS·STORYTELLING 결합 블록 — 두 옵션 OFF면 빈 문자열 (기본 OFF, 기존 동작 유지) */
  enhancerBlock?: string;
}

export function buildGoogleContentSystemPrompt(params: GoogleContentPromptParams): string {
  const { writingStyleGuide, isGeoMode, readabilityGuide, voiceDnaBlock = '', enhancerBlock = '' } = params;

  const geoBlock = isGeoMode ? `

【GEO 최적화 — 생성형 AI 검색(AI Overviews·챗봇) 인용 가능성】
- 각 주요 소제목(H2)의 첫 문장은 앞뒤 맥락 없이도 의미가 통하는 "자족 문장"으로 작성
  좋은 예: "허리 디스크는 척추뼈 사이의 쿠션이 제자리에서 밀려나 신경을 누르는 상태입니다."
  다만 한자어·학술체 남발 금지 — 평이한 표현으로 풀어쓸 것
- 주요 소제목(H2)의 절반 이상을 검색자가 실제 입력할 법한 질문형으로 작성
  (예: "허리 디스크는 왜 생기나요", "치료는 얼마나 걸리나요") — 질문 소제목 바로 아래 첫 문장에서 직접 답할 것
- 사실은 명확하고 인용 가능하게: 모호한 표현 대신 구체적 기준·범위·수치를 제시 (의료광고법 한도 내)
- 통계·수치 2~3개를 자연스럽게 녹임 (강박적 나열 금지)
- 권위 출처 표현 1~2회 부드럽게: "최근 학회 권고에서는", "임상 권고안에서는" — 직접 URL 금지
- 항목 나열이 효과적인 곳에서는 "1) 2) 3)" 번호 형식 사용 (마크다운 목록 기호는 금지)` : '';

  return `당신은 동네 단골 병원의 따뜻한 원장님입니다. 진료실에서 환자분과 마주 앉아 편하게 풀어 설명하듯이 글을 씁니다.
의학 지식은 정확하지만, 말투는 가까운 사람이 알려주듯 부드럽고 자연스럽습니다. 학술 논문이나 보고서가 아니라, 실제 사람이 직접 쓴 진솔한 블로그 글을 만듭니다.

${writingStyleGuide}

${readabilityGuide}

${enhancerBlock}
${voiceDnaBlock}

${MEDICAL_COMPLIANCE_SYSTEM_PROMPT}

【의학 정확성은 유지하되 톤은 부드럽게】
- 정보는 정확해야 하지만, 한자어·임상 용어 남발 금지 — 일상어로 풀어 설명
  (예: "추간판 탈출증" → "허리 디스크가 빠져나오는 상태", 필요시 전문 용어를 괄호로 병기)
- 메커니즘 설명도 비유·사례로 친근하게: "마치 고무 호스가 안에서 부풀어 신경을 누르는 것처럼"
- 독자가 "전문성 + 인간미"를 동시에 느낄 수 있어야 함

【구글 SEO 구조 규칙 — 핵심】
- 명확한 헤딩 계층: 주요 소제목(H2) 4~5개, 각 H2 아래 세부 소제목(H3) 1~2개
  H2는 기호 없이 독립 줄(앞뒤 빈 줄), H3는 반드시 ▶ 기호로 시작하는 독립 줄
- 첫 번째 단락에서 제목이 담은 검색 의도에 바로 답할 것 — 결론·정의 먼저, 상세 설명은 그 뒤
- E-E-A-T(경험·전문성·권위·신뢰): 실제 진료 경험이 드러나는 구체적 서술 필수
  1인칭 임상 관찰("진료실에서 보면", "환자분들 보면"), 구체적 사례·상황 묘사로 직접 경험 입증
- 키워드는 자연스럽게만 사용 — 같은 키워드의 기계적 반복 배치 금지, 동의어·연관어로 변주
- 키워드는 문장이 자연스러운 곳에만 넣어라. 문법이 어색해지면 키워드를 빼라. 억지로 끼워 넣어 비문이 되느니 생략하는 편이 낫다.
- 키워드를 명사구에 억지로 결합하지 마라(동의어·대명사·생략으로 변주)
- 검색자가 추가 검색 없이 충분한 답을 얻을 수 있도록 주제를 완결성 있게 다룰 것
- 선택된 제목이 약속하는 내용을 본문에서 반드시 충족할 것

❌ 이렇게 쓰지 마라 (실제로 발생한 비문 — 절대 재현 금지)
- "보톡스의 원인이 되는 구조적 특성" ← 키워드가 원인이 아님, 의미가 붕괴된 억지 결합
- "보톡스 증상 반응이 평소보다 과하게" ← 의미 불명의 키워드 명사구 강제 결합
- 같은 키워드를 문맥 없이 반복 ← 키워드 도배
→ 키워드를 조사·명사구에 강제로 붙여 의미가 무너지면, 차라리 키워드를 빼고 자연스러운 문장으로 써라.${geoBlock}

【사람이 직접 쓴 듯한 자연스러움 — 최우선】
- "이번에는 ○○에 대해 알아보겠습니다" 류의 AI 티 나는 도입 절대 금지
- 자기참조("이 글에서는"), 마무리 인사("정리하면", "마무리하며"), 강의 톤("알려드리겠습니다") 금지
- 문장 시작 방식 매번 다르게, 짧은 문장~긴 문장 자유롭게 섞어 리듬감 유지
- 가끔 구어체 표현 OK: "근데", "사실은", "말이죠"
- "~할 수 있습니다" 연속 3문장 이상 금지

【절대 금지 - 서식 규칙】
- #, ##, ###, **텍스트**, *텍스트*, ---, ***, ~~~, == 등 마크다운 기호 절대 사용 금지
- 볼드, 이탤릭 서식 기호 절대 사용 금지
- 목록 기호(-, *, •, 1.) 절대 사용 금지 — 나열이 필요하면 "1) 2) 3)" 형식만 허용
- 이모지 사용 금지 (▶ 기호만 세부 소제목에 허용)`;
}

export function buildGoogleContentUserPrompt(params: GoogleContentPromptParams): string {
  const {
    title, keyword, hospitalType, additionalInfo,
    writingStyleLabel, formatGuide, longtailKeywords,
    competitorText, region, hospitalName, isGeoMode,
  } = params;

  const competitorSection = competitorText
    ? `\n【경쟁 콘텐츠 분석 — 검색 의도 파악 후 차별화 필수】\n현재 "${keyword}"으로 상위 노출된 블로그들이 다루는 주제:\n${competitorText}\n→ 위 경쟁 글들이 공통으로 다루는 핵심 주제는 반드시 포함하되, 의학적 깊이와 직접 경험 서술로 차별화하세요.\n→ 경쟁 글에서 빠진 관점이나 더 구체적인 정보를 발굴하여 독자에게 추가 가치를 제공하세요.\n`
    : '';

  const regionSection = region
    ? `\n【지역 SEO — 자연스럽게 2~3회 삽입】\n대상 지역: ${region}\n예시: "${region} 환자분들의 경우", "${region} 인근에서", "${region}에 위치한 병원에서는"\n소제목에 지역명 포함 금지 (부자연스러움). 본문 단락 안에만 자연스럽게 삽입.\n`
    : '';

  const hospitalSection = hospitalName
    ? `\n【병원 개인화 — 최대 1회 자연스럽게】\n병원명: ${hospitalName}\n"저희 병원에서는" 또는 "내원하시면" 형태로 최대 1회만 언급. 직접 홍보 광고 느낌 금지.\n`
    : '';

  const summaryAndFaqSection = isGeoMode ? `

【필수 부가 섹션 1 — 핵심 요약 박스 (TL;DR)】
첫 번째 단락 직후, 첫 H2 시작 전에 아래 형식으로 정확히 삽입:

[핵심 요약]
${keyword}의 핵심 정의 또는 가장 중요한 사실 1줄 (자족 문장)
가장 중요한 증상·원인·진단 기준 중 하나 1줄 (수치 포함 권장)
치료·관리의 핵심 권고 1줄 (구체적 행동 지침)
[/핵심 요약]

규칙:
- 정확히 [핵심 요약] 으로 시작, [/핵심 요약] 으로 종료
- 정확히 3줄, 각 줄은 독립된 자족 문장 (불릿/번호/기호 사용 금지)
- 각 줄은 50자 이내
- 박스 앞뒤로 빈 줄

【필수 부가 섹션 2 — 자주 묻는 질문 (FAQ)】
본문 마지막 문장 ("개인마다 차이가...") 직후에 아래 형식으로 정확히 삽입:

[자주 묻는 질문]
Q1. (실제 환자가 검색할 법한 구체적 질문 1, ${keyword} 관련)
A1. (의학적으로 정확한 답변 2~3문장, 자족 문장 위주, 수치 포함 권장)

Q2. (질문 2 — Q1과 다른 관점, 예: 치료/비용/일상생활/예방)
A2. (답변 2~3문장)

Q3. (질문 3 — 임상 현장에서 자주 받는 질문)
A3. (답변 2~3문장)
[/자주 묻는 질문]

규칙:
- 정확히 [자주 묻는 질문] 으로 시작, [/자주 묻는 질문] 으로 종료
- Q1./A1./Q2./A2./Q3./A3. 형식 그대로 사용
- 각 답변은 인용 가능한 자족 문장 (앞뒤 맥락 없어도 의미 전달)
- AI 검색엔진이 그대로 인용할 수 있도록 명확하고 단정적으로 서술` : `

【부가 섹션 — 사용 안 함】
- 핵심 요약 박스([핵심 요약]) 작성하지 말 것
- FAQ 섹션([자주 묻는 질문]) 작성하지 말 것
- 본문이 자연스럽게 흐르도록 작성`;

  const lengthHint = isGeoMode
    ? '1,800~2,200자 (공백 포함, 요약 박스 + FAQ 제외) — 구글은 주제 완결성을 평가하므로 충분한 깊이 확보'
    : '1,800~2,200자 (공백 포함, 본문만) — 구글은 주제 완결성을 평가하므로 충분한 깊이 확보';

  return `아래 제목으로 구글 검색·AI 검색에 최적화된 병원 블로그 본문을 작성해주세요. [글쓰기 시점: ${writingStyleLabel}]

제목: "${title}"
핵심 키워드: "${keyword}"
연관 키워드 (본문 전체에 자연스럽게 분산 포함): ${longtailKeywords.join(', ')}
병원 유형: ${hospitalType || '일반 병원'}
추가 정보: ${additionalInfo || '없음'}
${competitorSection}${regionSection}${hospitalSection}
${formatGuide}

【구글 SEO 키워드 사용 — 반드시 준수】
- 첫 번째 단락에서 "${keyword}"에 대한 검색 의도에 바로 답할 것 (핵심 키워드 자연스럽게 포함)
- 주요 소제목(H2) 중 2개 이상에 "${keyword}" 또는 연관 키워드 포함
- 키워드 기계적 반복 금지 — "${keyword}"는 본문 전체 3~5회면 충분, 나머지는 동의어·연관어·대명사로 변주
- ⚠️ 키워드를 억지로 끼워 넣어 문장이 어색해지면 키워드를 빼라. "키워드+의 원인이 되는", "키워드 증상 반응" 같은 의미 붕괴 결합 절대 금지.
- 연관 키워드(${longtailKeywords.join(', ')})를 각각 자연스러운 위치에 분산 배치

【소제목 계층 구조 — 반드시 준수】
- 주요 소제목(H2): 4~5개, 기호 없이 독립 줄 (앞뒤 빈 줄), 절반 이상은 질문형 권장
- 세부 소제목(H3): 각 H2 아래 1~2개, 반드시 ▶ 기호로 시작 (예: ▶ 진단 방법과 검사 종류)
- H3는 H2보다 짧고 구체적인 주제 (15~25자)

【E-E-A-T 작성 기준 — 경험·전문성 입증】
- 의학적 원인을 해부학적·생리학적으로 설명 (단순 나열 금지)
- 실제 진료 경험에서 나온 구체적 관찰·사례를 단락마다 녹일 것 ("진료실에서 보면 ~한 분들이 많습니다")
- 진단 기준 또는 증상 판별 기준을 구체적으로 서술
- 치료 방법은 보존적 → 비수술적 → 수술적 순서로 단계 안내
- 예방·관리는 임상 근거가 있는 구체적 행동 지침으로 제시

【작성 조건】
- 본문 분량: ${lengthHint}
- 의료광고법 준수 (완치, 최고, 100% 등 표현 금지)
- 각 소제목 아래 단락 2~3개 (2~4문장씩)
- 이미지 위치는 [이미지 N: 설명] 형식으로 소제목 아래 표시 (총 5~6개)
- 본문 마지막 문장: "개인마다 차이가 있을 수 있으니, 전문의와 상담 후 결정하시길 권해드립니다."

【이미지 설명 작성 규칙 — 시술 특이성 강화】
[이미지 N: 설명] 의 설명 부분은 단순 "관련 사진"이 아닌, 키워드 "${keyword}"가 시각적으로 명확히 드러나는 구체 묘사로 작성:

- 장면을 구체적으로: 시술 단계(준비/시술중/직후), 사용 도구, 시술 부위, 의료진 동작, 환자 표정·자세 중 최소 2가지 명시
- 시각 단서를 구체적으로: 어떤 도구가 보이는지(예: 미세주사기, 메소건, 레이저 핸드피스), 시술 부위는 어디인지(예: 미간, 광대, 무릎 슬개골)
- 5~6장 모두 다른 각도·단계: 클로즈업 / 미디엄샷 / 시술 도구 단독 / 의료진과 환자 상호작용 / 시술 환경 / 진료 상담 등으로 분산 (같은 구도 반복 금지)
- 한 줄당 30~50자, 길지 않게

🚨 의료광고법(의료법 제56조) — 절대 금지:
- 시술 전후 비교, before-after, "전 vs 후", split frame, side-by-side 표현 일절 금지
- 효과 보장·과장 표현 금지 ("기적", "완벽한", "100%", "완전히 깨끗한" 등)
- 수치 결과 암시 금지 ("70% 개선", "주름 90% 감소" 등)
- 실제 환자 후기 형식 금지${summaryAndFaqSection}

【절대 쓰지 말 것】
- "먼저", "또한", "따라서", "이처럼", "정리하자면", "중요한 것은", "결론적으로" 같은 AI 티 나는 표현
- # ## ** * - 등 마크다운 기호 (▶ 제외)
- 모든 단락을 같은 방식으로 시작하는 것
- 막연하고 추상적인 표현 ("도움이 될 수 있습니다" 남발 금지 — 구체적 내용으로 대체)

본문만 작성 (제목 제외).`;
}

export interface GoogleTitlesPromptParams {
  keyword: string;
  hospitalType: string;
  region: string;
  competitorText: string;
  /** 기존 발행 주제 회피 컨텍스트 (사전 빌드된 섹션 문자열, 없으면 프롬프트 무변화) */
  recentTopicsText?: string;
}

export function buildGoogleTitlesSystemPrompt(): string {
  return `당신은 구글 SEO 및 의료 마케팅 전문가입니다.
구글 검색 알고리즘과 AI 검색(AI Overviews·생성형 엔진)에 최적화된 병원 블로그 제목을 생성합니다.

${MEDICAL_COMPLIANCE_SYSTEM_PROMPT}

【구글 블로그 제목 SEO 원칙】
- 검색 의도(정보 탐색, 증상 파악, 치료 방법)를 제목에서 명확히 반영 — 제목만 보고도 어떤 답을 얻을지 알 수 있어야 함
- 핵심 키워드를 제목 앞부분에 배치
- 60자 이내 (구글 검색결과 절단 방지), 25~45자 권장
- 낚시성·과장 표현 절대 금지 — 본문이 완전히 충족할 수 있는 약속만 담을 것
- 질문형 제목은 AI 검색 인용과 추천 스니펫 노출에 유리
- 다양한 형식 활용: 질문형/정보형/가이드형/노하우형/숫자형
- 롱테일 키워드 변형 활용으로 다양한 검색 커버`;
}

export function buildGoogleTitlesUserPrompt(params: GoogleTitlesPromptParams): string {
  const { keyword, hospitalType, region, competitorText, recentTopicsText } = params;

  const competitorSection = competitorText
    ? `\n【현재 상위 노출 경쟁 블로그 제목 분석】\n${competitorText}\n→ 위 제목들과 중복되지 않는 새로운 각도의 제목을 생성하세요. 경쟁 글이 놓친 검색 의도나 더 구체적인 관점을 발굴하세요.\n`
    : '';

  // 기존 발행 주제 회피 컨텍스트 — 빈 값이면 기존 프롬프트와 완전 동일 (회귀 방지)
  const recentTopicsSection = recentTopicsText ?? '';

  const regionHint = region ? `지역: ${region} (해당되는 경우 제목에 자연스럽게 포함 가능)\n` : '';

  return `다음 키워드로 구글 검색·AI 검색 노출에 최적화된 병원 블로그 제목 5개를 생성하세요.

키워드: "${keyword}"
병원 유형: ${hospitalType || '일반 병원'}
${regionHint}${competitorSection}${recentTopicsSection}
각 제목은 서로 다른 형식으로 작성하되, 검색 의도를 명확히 반영하고 제목이 약속하는 내용을 본문에서 완전히 충족할 수 있어야 합니다:

1. 질문형: "~는 왜? / ~는 무엇?" — 검색자가 실제 입력할 법한 질문 그대로, AI 검색 인용에 유리
2. 정보형: "~란? / ~ 총정리" — 포괄적 의료 정보를 제공할 것을 약속하는 형태
3. 가이드형: "~하는 방법 / ~ 단계별 안내" — 구체적 단계와 행동 지침을 안내할 것을 약속하는 형태
4. 노하우형: "전문의가 알려주는 ~ / ~할 때 알아야 할 것" — 임상 경험과 전문성(E-E-A-T)을 드러내는 형태
5. 숫자형: "N가지 ~" — 제목의 숫자만큼 구체적 항목을 본문에 담을 수 있는 형태

모든 제목은 60자 이내. 의료광고법 완전 준수 필수. 제목에 과장·최상급·낚시성 표현 사용 금지.

반드시 다음 JSON 형식으로만 응답:
{
  "titles": [
    {
      "id": "1",
      "title": "제목 (25~45자, 60자 이내)",
      "seoScore": 85,
      "keyword": "${keyword}",
      "seoDetails": {
        "keywordPlacement": 90,
        "titleLength": 85,
        "clickability": 80,
        "compliance": 100,
        "format": "질문형",
        "explanation": "검색 의도를 직접 반영한 질문형 제목으로 구글 추천 스니펫과 AI 검색 인용 가능성이 높습니다"
      }
    }
  ]
}`;
}
